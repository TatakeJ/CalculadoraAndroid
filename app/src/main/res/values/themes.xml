<!--declaración del archivo XML con codificación UTf-8-->
<?xml version="1.0" encoding="utf-8"?>
<!--elemento raiz del archivo resources-->
<resources xmlns:tools="http://schemas.android.com/tools">

    <!--Tema principal de la aplicación
        - name: identificador del tema (se usa en AndroidManifest).
        - parent: indica el tema base del cual hereda. En este caso:
        "Theme.AppCompat.Light.NoActionBar" = tema claro sin barra de acción.-->
    <style name="Theme.Calculadora_1" parent="Theme.AppCompat.Light.NoActionBar">

        <!--Definición de colores principales
            - colorPrimary: color principal de la app (barras, botones destacados).
            - colorPrimaryDark: variante más oscura del color principal (usada en status bar).
            - colorAccent: color secundario para resaltar controles (ej. botones, switches).
            Todos estos colores deben estar definidos en el archivo colors.xml.-->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>
    </style>
</resources>